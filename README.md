# IBU-ISC

A static website for the IBU International Student Conference.

## Run Locally

Open `index.html` in a browser.

## Project Structure

```text
index.html
styles.css
script.js
favicon/
images/
```

The original submitted archive is kept as `ibu-isc-master.zip`, but the website files are now extracted so the repository can be viewed and deployed directly.
